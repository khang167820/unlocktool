function updateCountdowns(){const now=Date.now();document.querySelectorAll('[data-expire]').forEach(el=>{const t=parseInt(el.dataset.expire)*1000;const d=t-now;if(d<=0){el.innerText='Đã hết hạn'}else{const h=Math.floor(d/3600000);const m=Math.floor((d%3600000)/60000);const s=Math.floor((d%60000)/1000);el.innerText=`${h}h ${m}m ${s}s`}});requestAnimationFrame(updateCountdowns)}window.onload=()=>requestAnimationFrame(updateCountdowns);$(document).ready(function(){$('.package-option').click(function(){$('.package-option').removeClass('selected');$(this).addClass('selected');$('#selected_price_id').val($(this).data('price-id'))});$('#rentButton').click(function(){if($('#selected_price_id').val()===''){alert('Vui lòng chọn một gói thuê!');return false}});$('#rentModal').on('show.bs.modal',function(event){var button=$(event.relatedTarget);var accountId=button.data('account-id');var modal=$(this);modal.find('#account_id').val(accountId)});const rowsPerPage=5;const $cards=$('.account-card');const totalPages=Math.ceil($cards.length/rowsPerPage);function showPage(page){$cards.hide();$cards.slice((page-1)*rowsPerPage,page*rowsPerPage).show()}showPage(1);for(let i=1;i<=totalPages;i++){$('<button>').text(i).click(()=>showPage(i)).appendTo('#pagination')}});